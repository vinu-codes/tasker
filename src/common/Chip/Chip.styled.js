import styled from 'styled-components'

const ChipContainer = styled.div`
  display: flex;
  flex-direction: row;
`

const ChipGroup = styled.ul`
  list-style: none;
  margin: 0;
  padding: 0;
`

const ChipItem = styled.li`
  list-style: none;
  margin: 0;
  padding: 0;
`

export { ChipContainer, ChipGroup, ChipItem }
